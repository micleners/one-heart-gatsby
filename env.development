# To start using this environment file, add a period before the file: `.env.development`.
# With this name it will become an environment file and be ignored by git.
# You don't want to commit this file to git because it will contain secret API keys.
# After changing the names, change the values below to your keys as found on contentful.
SPACE_ID=REPLACE_ME
API_KEY=REPLACE_ME

# If you use the `Content Delivery API - access token` for API_KEY, leave the next line commented
# If you use the `Content Preview API - access token` to view drafts for API_KEY, uncomment the next line commented
# HOST=preview.contentful.com